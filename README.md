# yohuman-get

CDN-hosted tester onboarding for **Yo Human** (installer + setup page).
Served via GitHub Pages so `curl | bash` is never bot-challenged.

Install:

```
curl -fsSL https://gitfischerai.github.io/yohuman-get/install.sh | bash -s -- YOUR-CODE
```
